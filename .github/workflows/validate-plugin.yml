name: validate
on: pull_request
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 -c "import json;d=json.load(open('plugins.json'));print('OK',len(d['plugins']))"